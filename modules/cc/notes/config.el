;;; -*- lexical-binding: t; no-byte-compile: t; -*-
;;; cc/notes/config.el

;; This module is intentionally split by responsibility.  These files are not
;; automatic Doom entry points, so each one is loaded explicitly here.
(load! "+org")
(load! "+roam")
(load! "+integrations")
